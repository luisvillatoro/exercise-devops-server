replicaCount: 1
image:
  repository: challenge12.azurecr.io/challenge-server

publicUrl: "challengedevops-server.duckdns.org"

specs:
  ingress:
    publicUrl: challengedevops-server.duckdns.org
    port: 80
  service:
    name: http
    port: 80
    protocol: TCP
    type: LoadBalancer